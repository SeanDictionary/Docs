# 国密

国密是国家密码管理局制定的密码算法标准，主要包括以下几种算法：

- SM1：对称加密算法，类似于AES，主要用于数据加密。闭源
- SM2：非对称加密算法，基于椭圆曲线密码学，主要用于数字签名和密钥交换。
- SM3：哈希算法，类似于SHA-256，主要用于数据完整性
- SM4：对称加密算法，类似于AES，主要用于数据加密。
- SM9：基于身份的密码算法，主要用于数字签名和密钥交换。